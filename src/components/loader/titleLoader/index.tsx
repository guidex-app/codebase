import { h } from 'preact';

const TitleLoader = () => (
  <div class="small_size_holder" style={{ padding: '60px 20px' }}>
    <div style={{ height: '40px', backgroundColor: '#2b303d', borderRadius: '20px' }} />
  </div>
);

export default TitleLoader;
